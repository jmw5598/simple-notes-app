import { MarkdownEasymdeDirective } from './markdown-easymde.directive';

describe('MarkdownEasymdeDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownEasymdeDirective();
    expect(directive).toBeTruthy();
  });
});
